CÁC BƯỚC PUSH
// Code clone git
git clone https://github.com/syhiendk35/syhiendk35.github.io.git

// ADD
git add --all
git commit -m "first commit"
git push -u origin master


Try this git command

git push origin master --force
or short of force -f

git push origin master -f
